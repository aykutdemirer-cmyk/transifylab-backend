# Versions below are what installs cleanly on Python 3.14 / Windows
# (prebuilt wheels only). On this host, create the venv with
# `python -m venv .venv --system-site-packages` so fastapi/pydantic-core
# come from the global interpreter (no cp314 wheel + no build toolchain).

# --- Core API ---
fastapi>=0.115
uvicorn[standard]>=0.34
python-multipart>=0.0.20
pydantic>=2.10
pydantic-settings>=2.7
aiofiles>=24.1

# --- Document conversion ---
# PDF -> Word (PyMuPDF for coordinate-based parsing + python-docx for grid construction)
pymupdf>=1.24.0
# Word/Text -> PDF
python-docx>=1.1
reportlab>=4.2

# --- Speech-to-Text (heavy; NOT installed by default) ---
# Uncomment the backend you want, or run e.g. `pip install faster-whisper`.
# Both are imported lazily; /api/v1/health reports which are available.
faster-whisper>=1.1        # local, offline (pulls in ctranslate2 + av/PyAV)
openai>=1.59              # OpenAI Whisper API
# PyAV: needed to transcode .gsm/.amr/.3gp uploads to WAV. Comes bundled with
# faster-whisper; install explicitly if you use only the OpenAI backend:
av>=11
# Speaker diarization (offline, no torch / no HF token). Also needs two ONNX
# models under backend/models/diarization/ (segmentation.onnx + embedding.onnx,
# see README):
#   sherpa-onnx>=1.13

# --- Tests ---
pytest>=8.3
pytest-asyncio>=0.25
httpx>=0.28
